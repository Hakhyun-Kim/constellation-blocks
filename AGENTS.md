# Constellation Defense — 에이전트 작업 지침

## 다른 컴퓨터에서 작업 이어가기

- 새 세션은 먼저 `docs/CONTINUATION.md`를 읽고, `git status --short`, `git log -1 --oneline`으로 인수 상태를 확인한다.
- Windows PowerShell에서는 `npm` 대신 `npm.cmd`를 쓴다. 처음에는 `npm.cmd ci`, 이어서 `npm.cmd run check`와 `node scripts/balance-check.mjs 60`을 실행한다.
- 마을 화면은 전투 화면의 변형이 아니다. `src/ui.js`의 이동·상호작용, `src/app/village-layout.js`의 순수 좌표/충돌, `src/gfx/village.js`의 시각 표현 경계를 지킨다. 마을에서는 방어 캔버스/HUD를 표시하거나 프레임하지 않는다.
- 이 저장소는 공개용이다. `D:\constellation-defense-submission`의 비공개 제출 문구·체크리스트·녹화물은 이 저장소에 복사하지 않는다.

## 제품 경계

- 이 게임의 공식 이름은 반드시 `Constellation Defense`다.
- 핵심 루프는 **실시간 3매치 → 선택한 보드 열의 3개 방어로에 전술 효과 적용**이다. Flare는 공격, Tide는 감속, Bloom은 회복·후퇴를 맡는다.
- 수학 문제를 풀어 진행하는 이전 프로토타입과는 분리한다. 새 기능·카피·공유 카드·저장 파일명·테스트에 수학 관문을 다시 넣지 않는다.

## 플레이북을 적용하는 방식

Agentic GameDev Playbook의 방법론은 채택한다. 새 기능은 아래 세 가지를 모두 통과해야 한다.

1. 외부 에셋 의존 없이 절차적 코드 또는 허용된 폰트로 만들 수 있는가?
2. 실제 플레이 중 의미 있는 선택을 늘리는가?
3. 순수 로직 또는 결정적인 테스트로 검증할 수 있는가?

플레이북의 Claude 플러그인 설정(`.claude-plugin/`, `.claude/`)은 이 저장소에 복사하지 않는다. Codex용으로는 이 파일과 프로젝트의 테스트 명령을 기준으로 작업한다. MCP 서버가 연결되어 있다고 가정하지도 않는다.


## 아키텍처 계약

- `src/balance/`: 밸런스 숫자와 곡선의 단일 출처다. 전술 수치도 이 계층에 둔다.
- `src/engine/`: DOM·Three.js·오디오를 모르는 순수 시뮬레이션이다. 상태를 바꾸고 이벤트를 반환한다.
- `src/tactics/`: DOM·엔진 없이 3매치 보드 생성·스왑·매치·보충·방어로 매핑만 맡는다.
- `src/engine/tactics.js`: 보드 구현을 모르며, `{ route, kind, size }` 전술 명령만 방어 상태와 이벤트로 해석한다.
- `src/app/`: 입력과 화면 흐름을 소유한다. 전술 보드의 화면 표현과 애니메이션 취소는 여기서 다룬다.
- `src/gfx/`, `src/ui.js`, `src/sfx.js`, `src/music.js`: 엔진 상태와 이벤트를 표현할 뿐, 게임 규칙을 결정하지 않는다.
- `src/main.js`: 배선과 고정 타임스텝을 담당한다. 전술 보드와 방어 엔진의 유일한 연결점이며, 규칙을 새로 넣지 않는다.

엔진에 랜덤성이 필요하면 `state.rng` 또는 주입된 RNG를 사용한다. 테스트에서는 시드 RNG를 사용하고, 렌더링 시간·DOM·전역 `Math.random()`에 결과가 좌우되지 않게 한다.

## 전술판 변경 규칙

- 유효한 스왑, 매치 탐지, 매치 그룹 분리, 보충, 연쇄 판정은 UI와 분리해 테스트 가능하게 유지한다.
- 한 번의 스왑으로 여러 독립 매치가 생기면 타입·크기·대상 방어로를 섞지 않는다. 각 매치를 명시적으로 처리하거나, 지원하지 않는 동작이면 그 사실을 UI와 테스트에 드러낸다.
- `castTactic()`을 수정하면 Flare/Tide/Bloom 각각의 정상 경로, 웨이브 밖 거부, 대상 방어로에 적이 없을 때, 4·5개 보정의 엔진 테스트를 추가한다.
- 지연된 연쇄나 애니메이션은 새 게임·불러오기·리셋 뒤의 보드를 건드리면 안 된다. 세션 토큰 또는 취소 가능한 타이머로 격리한다.
- 전술 보드가 저장 공정성에 영향을 주는 상태가 되면 보드·선택·보충 RNG 상태를 저장하거나, 저장 가능한 시점을 명확히 제한하고 테스트한다.

## 밸런싱과 검증

- 숫자를 바꾼 뒤에는 실제 플레이 규칙을 쓰는 봇과 기준선으로 확인한다. 봇이 사람에게 없는 정보나 행동을 쓰면 안 된다.
- 전술판이 전투 결과에 영향을 주므로, 밸런스 봇은 별지기 주문만 시전해서는 충분하지 않다. 전술 스왑/전술 시전 정책을 포함한 뒤에만 전술 밸런스 기준선으로 사용한다.
- 테스트는 기능 변경과 함께 추가한다. UI 스모크가 필요하면 `window.__game` 훅을 이용하되, 디버그 전용 동작이 게임 규칙을 우회하지 않게 한다.
- `dist/game.js`는 소스 수정 후 빌드 산출물로만 갱신한다. 수동 편집하지 않는다.
- 난이도는 감으로만 올리지 않는다. 플레이 규칙을 쓰는 봇으로 최소 60판씩 `쉬움/보통/어려움 × 초보/보통/고수`를 주기적으로 확인하고, 결과를 기준선과 함께 남긴다. 특히 보통 난이도의 초보 첫 원정 완수율이 0%에 수렴하거나 보통 플레이어가 25% 아래로 떨어지면 진입 난도가 높다고 보고 완화안을 먼저 검토한다.

## 플레이 감각의 지속 원칙

- 마을은 `dungeon100`의 걸어다니는 마을에서 검증된 감각을 따른다: 프레임 시간 기반 연속 이동, 축별 충돌 미끄러짐, 이동 방향 회전, 보행 바운스, 주인공을 부드럽게 따라가는 카메라, 근접 대상 강조와 이름표를 기본으로 한다. 캐릭터는 몸통·머리·머리카락·팔·눈·그림자가 구분되는 블록 실루엣을 사용한다.
- 전투가 끝나거나 다음 방어가 준비됐을 때 선택이 필요한 화면이 아니라면 짧은 카운트다운 뒤 다음 phase로 자동 진행한다. 카운트다운은 화면에 보이고, 직접 시작·지도/마을 조작·모달·일시정지는 자동 진행보다 우선한다.
- 고정 영웅단의 모든 영웅은 전투 중 카드를 클릭해 고유 액티브 능력을 사용할 수 있어야 한다. 능력의 쿨다운·대상·효과는 `src/balance/`와 순수 엔진에서 결정하고, UI는 선택과 피드백만 담당한다.
- 전체 화면 플래시·흔들림·비네트는 짧고 낮은 강도로 제한한다. 같은 순간 여러 전체 화면 효과를 중첩하지 않으며, `prefers-reduced-motion`과 게임 내 절제 효과 설정에서는 화면 플래시·큰 흔들림을 제거하고 국소 효과로 대체한다.

PowerShell에서는 실행 정책 때문에 `npm` 대신 `npm.cmd`를 사용한다.

```powershell
npm.cmd install
npm.cmd run engine:check
npm.cmd run tactics:check
node scripts/balance-check.mjs 60
npm.cmd run build
```

`npm.cmd run check`은 엔진 검사, 순수 3매치 보드 검사, 빌드를 실행한다. 밸런스 검사는 별도 게이트다. 의존성이 설치되지 않은 작업 공간에서는 빌드 성공을 주장하지 않는다.

## 출시 전 최소 검증

- 데스크톱과 모바일 폭에서 첫 스왑, 각 전술 효과, 4·5개 매치, 웨이브 시작·클리어, 웨이브 사이 강화, 패배 리셋을 직접 확인한다.
- 콘솔 오류와 누락 파일이 없어야 하며, 공개 Pages URL은 시크릿 창에서 로그인 없이 열려야 한다.
- 외부 이미지·오디오·3D 모델을 추가하지 않는다. 폰트를 새로 추가할 때는 라이선스와 폴백을 `CREDITS.md`에 기록한다.

## 작업 방식

- 변경 전 `git status --short`로 기존 사용자 변경을 확인하고, 관련 없는 변경은 건드리지 않는다.
- 한 기능의 규칙·밸런스·테스트·공개 설명이 함께 바뀌면 한 단위로 검토한다.
- 완료 보고에는 실행한 명령과 실제 결과, 아직 실행하지 못한 검증을 구분해서 적는다.
